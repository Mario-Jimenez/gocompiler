let globalNum = 10;

let sum = func(a, b) {
  c: a + b,
  c: c + globalNum
};

let outer = func() {
  sum : sum(1, 2) + sum(3, 4) + globalNum
};

print(outer() + globalNum);

if (true == false) {
  print("shouldn't happen");
} else {
  print("should happen");
}

// Single line comment
print("Nice") // Or like this

const cantChangeMe puts("neat");
print(cantChangeMe);

const moduloResult puts((21 / 8) / (8 / 3));
print(moduloResult);

let fullName = func(firstName, lastName) {
  fullName: puts(firstName + " " + lastName)
}

print(fullName("Thurman", "Murman"));

let anotherGlobalNum = 55;
anotherGlobalNum+1;
print(anotherGlobalNum);

let postfixPlusFunc = func() {
  localNum: 99,
  localNum: localNum+1,
  localNum: localNum
}

print(postfixPlusFunc());

let postfixMinusFunc = func() {
  localNum: 99,
  localNum: localNum-1,
  localNum: localNum
}

print(postfixMinusFunc());

/*
  ignored multiline comment print("will not print")
*/

print(5 >= 5)
print(5 <= 5)
print(5 > 5)
print(5 < 5)

/* ignored multiline comment */

print("string comparison below should print true false false")
let one = "one";
let two = "two";
print("monkey" == "monkey")
print("monkey" == "lang")
print(one == two)

print("pop some stuff");
print(pop([1, 2, 3]));
print(pop(["one", "two", "three"]));

let original = [1, 2, 3];
let newArray = pop(original);

print(original);
print(newArray);

let name = "hey there my name is brad";
let split_name = split(name, " ");
print(split_name);
let joined_name = join(split_name, " ");
print(joined_name);